/*
 Zbiór zadań z programowania w języku C/C++ cz. 2
 Jacek Krzaczkowski

 Zadanie 2.2
 
 Napisz klasę `liczba` służącą do przechowywania liczb wymiernych.
 Klasa `liczba` powinna posiadać publiczne pole `re` typu `double` oraz następujące metody publiczne:
    — wirtualną metodę `modul` zwracającą moduł przechowywanej liczby,
    — `wieksza` otrzymującą w argumencie referencję `ref` do obiektu klasy `liczba` i zwracający jako wartość
      `true` jeżeli moduł liczby `ref` jest większy od modułu liczby przechowywanej w obiekcie, na rzecz którego wywoływana 
      jest metoda oraz `false` w przeciwnym wypadku.
*/
